#include "holberton.h"
/**
 *
 * _islower - checks lowercase
 *@c: for integers
 * Return: Always 0 (Success)
 */
int _islower(int x)
{
if (x >='a' && x <= 'z')
{
return (1);
}
else
{
return (0);
}
} 
