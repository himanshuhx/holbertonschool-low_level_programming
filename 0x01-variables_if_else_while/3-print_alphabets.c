#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 *Retunr:Always 0 (Success)
 */
int main(void)
{
char x;
char y;

for (x = 'a'; x <= 'z'; x++)
{
putchar(x);
}
for (y = 'a'; y<= 'z'; y++)
{
putchar(y);
}
return (0);
}
